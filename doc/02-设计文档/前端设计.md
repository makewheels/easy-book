# Easy Book - 泳课学员管理系统前端设计文档

## 1. 项目概述

Easy Book前端采用Vue3 + Element Plus架构，为泳课学员管理系统提供简洁、高效的移动端用户界面，支持学员管理、预约管理、签到扣费等核心功能。

## 2. 技术栈

| 技术 | 版本 | 说明 |
|------|------|------|
| Vue.js | 3.x | 渐进式JavaScript框架 |
| Vite | 4.x | 快速构建工具 |
| Pinia | 2.x | 状态管理库 |
| Vue Router | 4.x | 路由管理 |
| Axios | 1.x | HTTP请求库 |

**注意**: 实际项目未使用Element Plus，采用自定义移动端UI组件

## 3. 项目结构

```
frontend/
├── public/
│   ├── index.html
│   └── favicon.ico
├── src/
│   ├── api/                    # API接口封装
│   │   ├── index.js            # 请求配置
│   │   ├── student.js          # 学员API
│   │   ├── appointment.js      # 预约API
│   │   └── attendance.js       # 签到API
│   ├── components/             # 通用组件
│   │   ├── common/
│   │   │   ├── NavBar.vue      # 底部导航
│   │   │   ├── Loading.vue     # 加载组件
│   │   │   └── EmptyState.vue  # 空状态组件
│   │   ├── student/
│   │   │   ├── StudentCard.vue # 学员卡片
│   │   │   ├── StudentForm.vue # 学员表单
│   │   │   └── StudentDetail.vue # 学员详情
│   │   └── appointment/
│   │       ├── AppointmentList.vue # 预约列表
│   │       ├── AppointmentItem.vue # 预约项
│   │       └── TimeSlot.vue    # 时间段组件
│   ├── views/                  # 页面组件
│   │   ├── Home.vue            # 首页-预约管理
│   │   ├── Students.vue        # 学生管理页面
│   │   ├── StudentDetail.vue   # 学生详情页面
│   │   ├── AddStudent.vue      # 新增学生页面
│   │   ├── EditStudent.vue     # 编辑学生页面
│   │   └── CalendarView.vue    # 表格日历页面
│   ├── router/
│   │   └── index.js            # 路由配置
│   ├── stores/                 # Pinia状态管理
│   │   ├── index.js
│   │   ├── student.js          # 学员状态
│   │   ├── appointment.js      # 预约状态
│   │   └── app.js              # 应用状态
│   ├── utils/                  # 工具函数
│   │   ├── date.js             # 日期工具
│   │   ├── request.js          # 请求工具
│   │   ├── constants.js        # 常量定义
│   │   └── validators.js       # 表单验证
│   ├── styles/                 # 样式文件
│   │   ├── index.scss          # 全局样式
│   │   ├── variables.scss      # 变量定义
│   │   └── mobile.scss         # 移动端样式
│   ├── App.vue                 # 根组件
│   └── main.js                 # 入口文件
├── package.json
├── vite.config.js              # Vite配置
├── .env                        # 环境变量
└── README.md
```

## 4. 核心组件设计

### 4.1 NavBar 底部导航组件

```vue
<!-- components/common/NavBar.vue -->
<template>
  <div class="bottom-nav">
    <div 
      class="nav-item" 
      :class="{ active: activeTab === 'home' }"
      @click="switchTab('home')"
    >
      <van-icon name="calendar-o" />
      <span>预约管理</span>
    </div>
    <div 
      class="nav-item" 
      :class="{ active: activeTab === 'students' }"
      @click="switchTab('students')"
    >
      <van-icon name="friends-o" />
      <span>学生管理</span>
    </div>
  </div>
</template>

<script setup>
import { defineProps, defineEmits } from 'vue'

const props = defineProps({
  activeTab: {
    type: String,
    default: 'home'
  }
})

const emit = defineEmits(['tab-change'])

const switchTab = (tab) => {
  emit('tab-change', tab)
}
</script>

<style lang="scss" scoped>
.bottom-nav {
  position: fixed;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 100%;
  max-width: 430px;
  height: 60px;
  background: #fff;
  border-top: 1px solid #eee;
  display: flex;
  align-items: center;
  justify-content: space-around;
  
  .nav-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    color: #666;
    font-size: 12px;
    cursor: pointer;
    transition: color 0.3s;
    
    .van-icon {
      font-size: 20px;
      margin-bottom: 2px;
    }
    
    &.active {
      color: #1989fa;
    }
  }
}
</style>
```

### 4.2 StudentCard 学员卡片组件

```vue
<!-- components/student/StudentCard.vue -->
<template>
  <div class="student-card" @click="goToDetail">
    <div class="card-header">
      <div class="student-info">
        <div class="name">{{ student.name }}</div>
        <div class="nickname" v-if="student.nickname">{{ student.nickname }}</div>
      </div>
      <button class="btn-appointment" @click.stop="quickAppointment">
        预约
      </button>
    </div>
    
    <div class="card-body">
      <div class="info-row">
        <span class="label">次数:</span>
        <span class="value">[{{ student.attended_lessons || 0 }}/{{ student.total_lessons }}]</span>
        <span class="type">{{ student.package_type }}</span>
      </div>
      <div class="info-row">
        <span class="label">项目:</span>
        <span class="value">{{ student.learning_item }}</span>
      </div>
      <div class="info-row">
        <span class="label">下次:</span>
        <span class="value">{{ student.next_appointment || '无预约' }}</span>
      </div>
    </div>
  </div>
</template>

<script setup>
import { defineProps, defineEmits } from 'vue'
import { useRouter } from 'vue-router'

const props = defineProps({
  student: {
    type: Object,
    required: true
  }
})

const emit = defineEmits(['quick-appointment'])
const router = useRouter()

const goToDetail = () => {
  router.push(`/student/${props.student.id}`)
}

const quickAppointment = () => {
  emit('quick-appointment', props.student)
}
</script>

<style lang="scss" scoped>
.student-card {
  background: #fff;
  border-radius: 8px;
  margin-bottom: 10px;
  padding: 15px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  cursor: pointer;
  transition: transform 0.2s;
  
  &:hover {
    transform: translateY(-2px);
  }
  
  .card-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 10px;
    
    .student-info {
      .name {
        font-size: 18px;
        font-weight: bold;
        color: #333;
      }
      
      .nickname {
        font-size: 14px;
        color: #666;
        margin-top: 2px;
      }
    }
    
    .btn-appointment {
      background: #1989fa;
      color: #fff;
      border: none;
      border-radius: 4px;
      padding: 6px 12px;
      font-size: 14px;
      cursor: pointer;
    }
  }
  
  .card-body {
    .info-row {
      display: flex;
      align-items: center;
      margin-bottom: 5px;
      font-size: 14px;
      
      .label {
        color: #666;
        margin-right: 5px;
        min-width: 40px;
      }
      
      .value {
        color: #333;
        flex: 1;
      }
      
      .type {
        background: #f0f9ff;
        color: #1989fa;
        padding: 2px 6px;
        border-radius: 4px;
        font-size: 12px;
        margin-left: 5px;
      }
    }
  }
}
</style>
```

### 4.3 TimeSlot 时间段组件

```vue
<!-- components/appointment/TimeSlot.vue -->
<template>
  <div class="time-slot">
    <div class="time-header">
      <span class="time">{{ time }}</span>
    </div>
    
    <div class="students">
      <div 
        v-for="student in students" 
        :key="student.id"
        class="student-item"
      >
        <div class="student-info">
          <div class="name-row">
            <span class="name">{{ student.name }}</span>
            <span class="type">{{ student.package_type }}</span>
            <span v-if="isPast" class="status" :class="statusClass(student.status)">
              {{ statusText(student.status) }}
            </span>
          </div>
          
          <div class="details">
            <span class="lessons">次数[{{ student.attended_lessons }}/{{ student.total_lessons }}]</span>
            <span class="project">{{ student.learning_item }}</span>
          </div>
        </div>
        
        <div v-if="!isPast && !student.status" class="actions">
          <button class="btn-checkin" @click="checkIn(student)">
            签到
          </button>
          <button class="btn-absent" @click="markAbsent(student)">
            缺席
          </button>
        </div>
      </div>
    </div>
  </div>
</template>

<script setup>
import { defineProps, defineEmits } from 'vue'

const props = defineProps({
  time: {
    type: String,
    required: true
  },
  students: {
    type: Array,
    default: () => []
  },
  isPast: {
    type: Boolean,
    default: false
  }
})

const emit = defineEmits(['checkin', 'absent'])

const statusClass = (status) => {
  return {
    'status-checked': status === 'checked',
    'status-absent': status === 'absent'
  }
}

const statusText = (status) => {
  const statusMap = {
    'checked': '已签到',
    'absent': '已缺席'
  }
  return statusMap[status] || ''
}

const checkIn = (student) => {
  emit('checkin', student)
}

const markAbsent = (student) => {
  emit('absent', student)
}
</script>

<style lang="scss" scoped>
.time-slot {
  background: #fff;
  border-radius: 8px;
  margin-bottom: 10px;
  overflow: hidden;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  
  .time-header {
    background: #f8f9fa;
    padding: 12px;
    border-bottom: 2px solid #eee;
    
    .time {
      font-size: 22px;
      font-weight: bold;
      color: #1989fa;
    }
  }
  
  .students {
    .student-item {
      padding: 12px;
      border-bottom: 1px solid #f0f0f0;
      
      &:last-child {
        border-bottom: none;
      }
      
      .student-info {
        .name-row {
          display: flex;
          align-items: center;
          margin-bottom: 5px;
          
          .name {
            font-size: 16px;
            font-weight: bold;
            color: #333;
            margin-right: 8px;
          }
          
          .type {
            background: #f0f9ff;
            color: #1989fa;
            padding: 2px 6px;
            border-radius: 4px;
            font-size: 12px;
            margin-right: 8px;
          }
          
          .status {
            font-size: 12px;
            padding: 2px 6px;
            border-radius: 4px;
            
            &.status-checked {
              background: #e6f7e6;
              color: #52c41a;
            }
            
            &.status-absent {
              background: #fff2e8;
              color: #fa8c16;
            }
          }
        }
        
        .details {
          display: flex;
          justify-content: space-between;
          font-size: 14px;
          color: #666;
          
          .lessons {
            margin-right: 10px;
          }
        }
      }
      
      .actions {
        display: flex;
        gap: 10px;
        margin-top: 10px;
        
        button {
          flex: 1;
          padding: 8px;
          border: none;
          border-radius: 4px;
          font-size: 14px;
          cursor: pointer;
          
          &.btn-checkin {
            background: #52c41a;
            color: #fff;
          }
          
          &.btn-absent {
            background: #fa8c16;
            color: #fff;
          }
        }
      }
    }
  }
}
</style>
```

## 5. 状态管理设计

### 5.1 学员状态管理

```javascript
// stores/student.js
import { defineStore } from 'pinia'
import { studentApi } from '@/api/student'

export const useStudentStore = defineStore('student', {
  state: () => ({
    students: [],
    currentStudent: null,
    loading: false,
    error: null
  }),
  
  getters: {
    totalStudents: (state) => state.students.length,
    activeStudents: (state) => state.students.filter(s => s.remaining_lessons > 0),
    getStudentById: (state) => (id) => state.students.find(s => s.id === id)
  },
  
  actions: {
    async fetchStudents() {
      this.loading = true
      this.error = null
      
      try {
        const response = await studentApi.getAll()
        this.students = response.data
      } catch (error) {
        this.error = error.message
        throw error
      } finally {
        this.loading = false
      }
    },
    
    async createStudent(studentData) {
      this.loading = true
      
      try {
        const response = await studentApi.create(studentData)
        this.students.push(response.data)
        return response.data
      } catch (error) {
        this.error = error.message
        throw error
      } finally {
        this.loading = false
      }
    },
    
    async updateStudent(id, data) {
      this.loading = true
      
      try {
        const response = await studentApi.update(id, data)
        const index = this.students.findIndex(s => s.id === id)
        if (index !== -1) {
          this.students[index] = response.data
        }
        
        if (this.currentStudent?.id === id) {
          this.currentStudent = response.data
        }
        
        return response.data
      } catch (error) {
        this.error = error.message
        throw error
      } finally {
        this.loading = false
      }
    },
    
    async deleteStudent(id) {
      this.loading = true
      
      try {
        await studentApi.delete(id)
        this.students = this.students.filter(s => s.id !== id)
        
        if (this.currentStudent?.id === id) {
          this.currentStudent = null
        }
      } catch (error) {
        this.error = error.message
        throw error
      } finally {
        this.loading = false
      }
    },
    
    setCurrentStudent(student) {
      this.currentStudent = student
    },
    
    clearError() {
      this.error = null
    }
  }
})
```

### 5.2 预约状态管理

```javascript
// stores/appointment.js
import { defineStore } from 'pinia'
import { appointmentApi } from '@/api/appointment'
import { getToday, formatDate } from '@/utils/date'

export const useAppointmentStore = defineStore('appointment', {
  state: () => ({
    appointments: [],
    todayAppointments: [],
    selectedDate: getToday(),
    loading: false,
    error: null
  }),
  
  getters: {
    appointmentsByDate: (state) => {
      const grouped = {}
      
      state.appointments.forEach(apt => {
        const dateKey = formatDate(apt.appointment_date)
        if (!grouped[dateKey]) {
          grouped[dateKey] = {
            date: dateKey,
            weekday: getWeekday(apt.appointment_date),
            isPast: new Date(apt.appointment_date) < new Date(),
            slots: {}
          }
        }
        
        const timeSlot = apt.time_slot
        if (!grouped[dateKey].slots[timeSlot]) {
          grouped[dateKey].slots[timeSlot] = []
        }
        
        grouped[dateKey].slots[timeSlot].push(apt)
      })
      
      // 转换为数组并排序
      return Object.values(grouped).map(date => ({
        ...date,
        slots: Object.entries(date.slots)
          .map(([time, students]) => ({ time, students }))
          .sort((a, b) => a.time.localeCompare(b.time))
      })).sort((a, b) => new Date(a.date) - new Date(b.date))
    }
  },
  
  actions: {
    async fetchDailyAppointments(date = this.selectedDate) {
      this.loading = true
      this.error = null
      
      try {
        const response = await appointmentApi.getDaily(date)
        this.appointments = response.data.slots.flat()
        this.selectedDate = date
        
        if (date === getToday()) {
          this.todayAppointments = this.appointments
        }
      } catch (error) {
        this.error = error.message
        throw error
      } finally {
        this.loading = false
      }
    },
    
    async createAppointment(appointmentData) {
      this.loading = true
      
      try {
        const response = await appointmentApi.create(appointmentData)
        this.appointments.push(response.data)
        
        // 刷新当日预约
        await this.fetchDailyAppointments(this.selectedDate)
        
        return response.data
      } catch (error) {
        this.error = error.message
        throw error
      } finally {
        this.loading = false
      }
    },
    
    async updateAppointment(id, data) {
      this.loading = true
      
      try {
        const response = await appointmentApi.update(id, data)
        const index = this.appointments.findIndex(a => a.id === id)
        if (index !== -1) {
          this.appointments[index] = response.data
        }
        
        return response.data
      } catch (error) {
        this.error = error.message
        throw error
      } finally {
        this.loading = false
      }
    },
    
    async deleteAppointment(id) {
      this.loading = true
      
      try {
        await appointmentApi.delete(id)
        this.appointments = this.appointments.filter(a => a.id !== id)
      } catch (error) {
        this.error = error.message
        throw error
      } finally {
        this.loading = false
      }
    },
    
    setSelectedDate(date) {
      this.selectedDate = date
    },
    
    clearError() {
      this.error = null
    }
  }
})
```

## 6. 页面组件设计

### 6.1 首页 - 预约管理

```vue
<!-- views/Home.vue -->
<template>
  <div class="home-page">
    <div class="header">
      <h1>泳课预约系统</h1>
      <div class="date-selector">
        <van-button 
          size="small" 
          @click="selectDate"
        >
          {{ selectedDateText }}
        </van-button>
      </div>
    </div>
    
    <div class="content">
      <van-loading v-if="loading" />
      
      <div v-else-if="appointmentsByDate.length === 0" class="empty-state">
        <van-empty description="暂无预约" />
      </div>
      
      <div v-else>
        <div 
          v-for="dateGroup in appointmentsByDate" 
          :key="dateGroup.date"
          class="date-group"
        >
          <div class="date-header">
            {{ dateGroup.date }} {{ dateGroup.weekday }}
          </div>
          
          <TimeSlot 
            v-for="slot in dateGroup.slots" 
            :key="slot.time"
            :time="slot.time"
            :students="slot.students"
            :is-past="dateGroup.isPast"
            @checkin="handleCheckIn"
            @absent="handleAbsent"
          />
        </div>
      </div>
    </div>
    
    <NavBar active-tab="home" @tab-change="handleTabChange" />
    
    <!-- 日期选择器 -->
    <van-calendar 
      v-model:show="showDatePicker" 
      @confirm="onDateConfirm"
      :min-date="minDate"
      :max-date="maxDate"
    />
  </div>
</template>

<script setup>
import { ref, computed, onMounted } from 'vue'
import { useAppointmentStore } from '@/stores/appointment'
import { useAttendanceStore } from '@/stores/attendance'
import { getToday, formatDateText } from '@/utils/date'
import NavBar from '@/components/common/NavBar.vue'
import TimeSlot from '@/components/appointment/TimeSlot.vue'

const appointmentStore = useAppointmentStore()
const attendanceStore = useAttendanceStore()

const showDatePicker = ref(false)
const minDate = new Date()
const maxDate = new Date()
maxDate.setDate(maxDate.getDate() + 30)

const loading = computed(() => appointmentStore.loading)
const appointmentsByDate = computed(() => appointmentStore.appointmentsByDate)
const selectedDateText = computed(() => 
  formatDateText(appointmentStore.selectedDate)
)

onMounted(() => {
  appointmentStore.fetchDailyAppointments()
})

const selectDate = () => {
  showDatePicker.value = true
}

const onDateConfirm = (date) => {
  const dateStr = date.toISOString().split('T')[0]
  appointmentStore.setSelectedDate(dateStr)
  appointmentStore.fetchDailyAppointments(dateStr)
  showDatePicker.value = false
}

const handleCheckIn = async (student) => {
  try {
    await attendanceStore.checkIn(student.id, student.appointment_id)
    await appointmentStore.fetchDailyAppointments(appointmentStore.selectedDate)
  } catch (error) {
    console.error('签到失败:', error)
  }
}

const handleAbsent = async (student) => {
  try {
    await attendanceStore.markAbsent(student.id, student.appointment_id)
    await appointmentStore.fetchDailyAppointments(appointmentStore.selectedDate)
  } catch (error) {
    console.error('标记缺席失败:', error)
  }
}

const handleTabChange = (tab) => {
  if (tab === 'students') {
    router.push('/students')
  }
}
</script>

<style lang="scss" scoped>
.home-page {
  min-height: 100vh;
  background: #f5f5f5;
  padding-bottom: 60px;
  
  .header {
    background: #1989fa;
    color: #fff;
    padding: 15px;
    position: sticky;
    top: 0;
    z-index: 100;
    
    h1 {
      font-size: 22px;
      margin: 0 0 10px 0;
    }
    
    .date-selector {
      .van-button {
        background: rgba(255, 255, 255, 0.2);
        color: #fff;
        border: 1px solid rgba(255, 255, 255, 0.3);
      }
    }
  }
  
  .content {
    padding: 10px;
    
    .empty-state {
      text-align: center;
      padding: 50px 0;
    }
    
    .date-group {
      margin-bottom: 20px;
      
      .date-header {
        background: #f8f9fa;
        padding: 12px;
        font-weight: bold;
        color: #1989fa;
        border-radius: 8px;
        margin-bottom: 15px;
        font-size: 18px;
      }
    }
  }
}
</style>
```

### 6.2 学生管理页面

```vue
<!-- views/Students.vue -->
<template>
  <div class="students-page">
    <div class="header">
      <h1>学生管理</h1>
      <div class="stats">
        共 {{ totalStudents }} 人，活跃 {{ activeStudents }} 人
      </div>
    </div>
    
    <div class="content">
      <van-loading v-if="loading" />
      
      <div v-else-if="students.length === 0" class="empty-state">
        <van-empty description="暂无学生">
          <van-button type="primary" @click="goToAddStudent">
            添加学生
          </van-button>
        </van-empty>
      </div>
      
      <div v-else>
        <StudentCard 
          v-for="student in students" 
          :key="student.id"
          :student="student"
          @quick-appointment="handleQuickAppointment"
        />
        
        <div class="add-student-btn" @click="goToAddStudent">
          <van-icon name="plus" />
          <span>新增学生</span>
        </div>
      </div>
    </div>
    
    <NavBar active-tab="students" @tab-change="handleTabChange" />
    
    <!-- 快速预约弹窗 -->
    <van-popup 
      v-model:show="showAppointmentDialog" 
      position="bottom"
      :style="{ height: '60%' }"
    >
      <div class="appointment-dialog">
        <div class="dialog-header">
          <h3>预约课程 - {{ selectedStudent?.name }}</h3>
          <van-button size="small" @click="showAppointmentDialog = false">
            取消
          </van-button>
        </div>
        
        <div class="dialog-content">
          <AppointmentForm 
            :student="selectedStudent"
            @submit="handleAppointmentSubmit"
            @cancel="showAppointmentDialog = false"
          />
        </div>
      </div>
    </van-popup>
  </div>
</template>

<script setup>
import { ref, computed, onMounted } from 'vue'
import { useRouter } from 'vue-router'
import { useStudentStore } from '@/stores/student'
import { useAppointmentStore } from '@/stores/appointment'
import NavBar from '@/components/common/NavBar.vue'
import StudentCard from '@/components/student/StudentCard.vue'
import AppointmentForm from '@/components/appointment/AppointmentForm.vue'

const router = useRouter()
const studentStore = useStudentStore()
const appointmentStore = useAppointmentStore()

const showAppointmentDialog = ref(false)
const selectedStudent = ref(null)

const loading = computed(() => studentStore.loading)
const students = computed(() => studentStore.students)
const totalStudents = computed(() => studentStore.totalStudents)
const activeStudents = computed(() => studentStore.activeStudents.length)

onMounted(() => {
  studentStore.fetchStudents()
})

const goToAddStudent = () => {
  router.push('/add-student')
}

const handleQuickAppointment = (student) => {
  selectedStudent.value = student
  showAppointmentDialog.value = true
}

const handleAppointmentSubmit = async (appointmentData) => {
  try {
    await appointmentStore.createAppointment(appointmentData)
    showAppointmentDialog.value = false
    selectedStudent.value = null
  } catch (error) {
    console.error('预约失败:', error)
  }
}

const handleTabChange = (tab) => {
  if (tab === 'home') {
    router.push('/')
  }
}
</script>

<style lang="scss" scoped>
.students-page {
  min-height: 100vh;
  background: #f5f5f5;
  padding-bottom: 60px;
  
  .header {
    background: #1989fa;
    color: #fff;
    padding: 15px;
    position: sticky;
    top: 0;
    z-index: 100;
    
    h1 {
      font-size: 22px;
      margin: 0 0 10px 0;
    }
    
    .stats {
      font-size: 14px;
      opacity: 0.8;
    }
  }
  
  .content {
    padding: 10px;
    
    .empty-state {
      text-align: center;
      padding: 50px 0;
    }
    
    .add-student-btn {
      background: #1989fa;
      color: #fff;
      border: none;
      border-radius: 8px;
      padding: 15px;
      text-align: center;
      margin: 20px 0;
      cursor: pointer;
      font-size: 16px;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
    }
  }
  
  .appointment-dialog {
    height: 100%;
    display: flex;
    flex-direction: column;
    
    .dialog-header {
      padding: 15px;
      border-bottom: 1px solid #eee;
      display: flex;
      justify-content: space-between;
      align-items: center;
      
      h3 {
        margin: 0;
        font-size: 18px;
      }
    }
    
    .dialog-content {
      flex: 1;
      padding: 15px;
      overflow-y: auto;
    }
  }
}
</style>
```

## 7. 工具函数设计

### 7.1 日期工具

```javascript
// utils/date.js
export function formatDate(date) {
  const d = new Date(date)
  const month = String(d.getMonth() + 1).padStart(2, '0')
  const day = String(d.getDate()).padStart(2, '0')
  return `${month}-${day}`
}

export function formatDateText(date) {
  const d = new Date(date)
  const weekdays = ['周日', '周一', '周二', '周三', '周四', '周五', '周六']
  return `${formatDate(date)} ${weekdays[d.getDay()]}`
}

export function formatTime(time) {
  return time.slice(0, 5)
}

export function getToday() {
  return new Date().toISOString().split('T')[0]
}

export function getWeekday(date) {
  const weekdays = ['周日', '周一', '周二', '周三', '周四', '周五', '周六']
  return weekdays[new Date(date).getDay()]
}

export function isPastDate(date) {
  return new Date(date) < new Date().setHours(0, 0, 0, 0)
}

export function addDays(date, days) {
  const result = new Date(date)
  result.setDate(result.getDate() + days)
  return result.toISOString().split('T')[0]
}
```

### 7.2 请求工具

```javascript
// utils/request.js
import axios from 'axios'
import { showToast } from 'vant'

const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || 'http://localhost:8000',
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json'
  }
})

// 请求拦截器
request.interceptors.request.use(
  (config) => {
    // 可以在这里添加token等认证信息
    return config
  },
  (error) => {
    return Promise.reject(error)
  }
)

// 响应拦截器
request.interceptors.response.use(
  (response) => {
    const { data } = response
    
    // 统一处理API响应格式
    if (data.code !== undefined && data.code !== 200) {
      showToast(data.message || '请求失败')
      return Promise.reject(new Error(data.message || '请求失败'))
    }
    
    return data
  },
  (error) => {
    let message = '网络错误'
    
    if (error.response) {
      const { status, data } = error.response
      
      switch (status) {
        case 400:
          message = data.message || '请求参数错误'
          break
        case 401:
          message = '未授权'
          break
        case 403:
          message = '禁止访问'
          break
        case 404:
          message = '资源不存在'
          break
        case 409:
          message = data.message || '数据冲突'
          break
        case 500:
          message = '服务器错误'
          break
        default:
          message = data.message || `请求失败 (${status})`
      }
    }
    
    showToast(message)
    return Promise.reject(error)
  }
)

export default request
```

### 7.3 常量定义

```javascript
// utils/constants.js

// 学习项目选项
export const LEARNING_ITEMS = [
  { value: '蛙泳', label: '蛙泳' },
  { value: '自由泳', label: '自由泳' },
  { value: '仰泳', label: '仰泳' },
  { value: '蝶泳', label: '蝶泳' },
  { value: '踩水', label: '踩水' }
]

// 套餐类型选项
export const PACKAGE_TYPES = [
  { value: '1v1', label: '1v1' },
  { value: '1v多', label: '1v多' }
]

// 时间段选项
export const TIME_SLOTS = [
  { value: '08:00', label: '08:00' },
  { value: '09:00', label: '09:00' },
  { value: '10:00', label: '10:00' },
  { value: '11:00', label: '11:00' },
  { value: '14:00', label: '14:00' },
  { value: '15:00', label: '15:00' },
  { value: '16:00', label: '16:00' },
  { value: '17:00', label: '17:00' }
]

// 预约状态
export const APPOINTMENT_STATUS = {
  SCHEDULED: 'scheduled',
  CHECKED: 'checked',
  ABSENT: 'absent'
}

// 出勤状态
export const ATTENDANCE_STATUS = {
  CHECKED: 'checked',
  ABSENT: 'absent'
}
```

## 8. 移动端适配

### 8.1 响应式配置

```scss
// styles/mobile.scss
// 移动端适配基础样式
.app-container {
  max-width: 430px;
  margin: 0 auto;
  min-height: 100vh;
  background: #f5f5f5;
  position: relative;
}

// 响应式字体大小
html {
  font-size: 16px;
  
  @media (max-width: 430px) {
    font-size: 14px;
  }
}

// 移动端触摸优化
button, .btn {
  min-height: 44px; // iOS推荐的最小触摸目标尺寸
  touch-action: manipulation; // 禁用双击缩放
}

// 滚动优化
.content {
  -webkit-overflow-scrolling: touch; // iOS平滑滚动
  scroll-behavior: smooth;
}

// 输入框优化
input, textarea {
  font-size: 16px; // 防止iOS缩放
  -webkit-appearance: none; // 移除默认样式
  border-radius: 0;
}

// 安全区域适配
.safe-area-bottom {
  padding-bottom: constant(safe-area-inset-bottom);
  padding-bottom: env(safe-area-inset-bottom);
}

// 底部导航适配
.bottom-nav {
  padding-bottom: constant(safe-area-inset-bottom);
  padding-bottom: env(safe-area-inset-bottom);
}
```

### 8.2 Vite配置

```javascript
// vite.config.js
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')
    }
  },
  server: {
    host: '0.0.0.0',
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true
      }
    }
  },
  build: {
    target: 'es2015',
    outDir: 'dist',
    assetsDir: 'assets',
    sourcemap: false,
    rollupOptions: {
      output: {
        manualChunks: {
          'element-plus': ['element-plus'],
          'vendor': ['vue', 'vue-router', 'pinia']
        }
      }
    }
  }
})
```

## 9. 性能优化

### 9.1 组件懒加载

```javascript
// router/index.js
import { createRouter, createWebHistory } from 'vue-router'

const routes = [
  {
    path: '/',
    name: 'Home',
    component: () => import('@/views/Home.vue')
  },
  {
    path: '/students',
    name: 'Students',
    component: () => import('@/views/Students.vue')
  },
  {
    path: '/student/:id',
    name: 'StudentDetail',
    component: () => import('@/views/StudentDetail.vue')
  },
  {
    path: '/add-student',
    name: 'AddStudent',
    component: () => import('@/views/AddStudent.vue')
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

export default router
```

### 9.2 虚拟滚动

```vue
<!-- 大列表虚拟滚动组件 -->
<template>
  <div class="virtual-list" :style="{ height: containerHeight + 'px' }">
    <div class="virtual-list-phantom" :style="{ height: totalHeight + 'px' }"></div>
    <div class="virtual-list-content" :style="{ transform: `translateY(${offsetY}px)` }">
      <div 
        v-for="item in visibleItems" 
        :key="item.id"
        class="virtual-list-item"
        :style="{ height: itemHeight + 'px' }"
      >
        <slot :item="item" :index="item.index"></slot>
      </div>
    </div>
  </div>
</template>

<script setup>
import { ref, computed, onMounted, onUnmounted } from 'vue'

const props = defineProps({
  items: Array,
  itemHeight: {
    type: Number,
    default: 60
  },
  containerHeight: {
    type: Number,
    default: 400
  }
})

const scrollTop = ref(0)
const containerRef = ref(null)

const totalHeight = computed(() => props.items.length * props.itemHeight)
const visibleCount = computed(() => Math.ceil(props.containerHeight / props.itemHeight) + 2)
const startIndex = computed(() => Math.floor(scrollTop.value / props.itemHeight))
const endIndex = computed(() => Math.min(startIndex.value + visibleCount.value, props.items.length))
const offsetY = computed(() => startIndex.value * props.itemHeight)

const visibleItems = computed(() => {
  return props.items.slice(startIndex.value, endIndex.value).map((item, index) => ({
    ...item,
    index: startIndex.value + index
  }))
})

const handleScroll = (e) => {
  scrollTop.value = e.target.scrollTop
}

onMounted(() => {
  containerRef.value?.addEventListener('scroll', handleScroll)
})

onUnmounted(() => {
  containerRef.value?.removeEventListener('scroll', handleScroll)
})
</script>
```

---

*Easy Book - 泳课学员管理系统前端设计文档*